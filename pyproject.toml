[project]
name = "lemma-tracing-monorepo"
version = "0.0.0"
description = "Monorepo root – not published"
requires-python = ">=3.11"

[tool.uv.workspace]
members = ["packages/py/*"]

[tool.uv.sources]
uselemma-tracing = { workspace = true }
uselemma-experiments = { workspace = true }
